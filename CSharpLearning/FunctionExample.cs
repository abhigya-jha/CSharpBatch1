using System;
namespace CSharpLearning
{
    public class FunctionExample
    {
        //Write a function which contains 2 int values
        //I will add those 2 value and print the result in the same function
        /*
         * Requirement--> add 2 values and print result
         * pre-requisite --> 2 int values
         * logic--> do addition of those 2 values-->store the result in a variable->print
         * 
         */

        public void addtwoValues()
        {
            int i = 20;
            int j = 20;

            int result=i + j;
            Console.WriteLine(result);
             
        }

        /* 
         * application
         * documents
         * photographs
         * fees
         * 
         * 
         * <rollNumber> getAdmission(application,documents, photo,fees){
         * 
         *    verifications
         *    counting 
         *    doing the admission
         * }
         */

       
    }
}
